#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>

using namespace std;

int main() {
    int m,n;
    cin >> m >> n;
    if (m>=1 && n>=1 && m<=16 && n<=16) {
        if (m<=n) {
            vector<int> nums(n);
        }
    }
}