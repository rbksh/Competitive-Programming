#include <bits/stdc++.h>
using namespace std;

int main() {
    int x;
    int y;
    cin >> x >> y;
    cout << x*y << endl;
}
//problem was made unnecessarily complex to confuse the solver. basic multiplication laws which we learned in PNC are used here. 